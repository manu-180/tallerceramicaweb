import 'package:flutter/material.dart';

class RutasTalleres extends StatelessWidget {
  const RutasTalleres({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Seleccione su taller'),
      ),
    );
  }
}